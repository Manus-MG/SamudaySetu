# Base URL for the API. Leave as-is in development: Vite proxies /api to
# 127.0.0.1:4000 (see vite.config.ts). In production set the full origin,
# e.g. https://api.samudaysetu.in/api/v1
VITE_API_BASE_URL=/api/v1
